{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "test:calculate-auth": "pnpm --filter @workspace/api-server test:calculate-auth",
    "test:calculators": "pnpm --filter @workspace/idalia test:calculators && pnpm run test:calculators:visual",
    "test:calculators:visual": "cd artifacts/idalia && PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=\"${PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH:-$(command -v chromium || true)}\" playwright test --config playwright.visual.config.ts",
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@testing-library/dom": "^10.4.2",
    "@testing-library/jest-dom": "^7.0.1",
    "@testing-library/user-event": "^14.6.7",
    "@types/supertest": "^7.2.1",
    "jsdom": "^30.1.1",
    "prettier": "^3.8.3",
    "supertest": "^7.3.0",
    "typescript": "~5.9.3"
  }
}
